<%- include ('partials/header.ejs')%>

    <!-- <h1><%= meal.name %></h1>
    <img src="<%= meal.image %>" alt="<%= meal.name %>">
    <p><%= meal.description %></p> -->


    <div class="ui main text container segment">
        <div class="ui huge header"><%= meal.name %></div>
        <hr>
        <div class="ui top attached">
            <div class="item">
                <img src="<%= meal.image %>" alt="<%= meal.name %>" class="ui fluid rounded image">
            </div>
            <br>
            <div class="description">
                <%= meal.description %>
            </div>
        </div>
        <a href="" class="ui inverted green button">Edit</a>
        <form id="delete" action="/meals/<%= meal._id %>?_method=DELETE" method="POST">
            <button type="submit" class="ui inverted red button">Delete</button>
        </form>
    </div>

<%- include ('partials/footer.ejs')%>